{
  "name": "obs-websocket-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP wrapper that replaces the Thrift plugin and drives OBS via obs-websocket",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "obs-websocket-js": "^5.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
